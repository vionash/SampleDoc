example:
  preview_url: true
  text:
    body: 'Check out this site: http://www.telnyx.com'
  to: '15125557878'
  type: text
properties:
  audio:
    $ref: ./Audio.yaml
  document:
    $ref: ./Document.yaml
  image:
    $ref: ./Image.yaml
  location:
    $ref: ./Location.yaml
  preview_url:
    description: >-
      Specifying preview_url in the request is optional when not including a URL
      in your message.

      To include a URL preview, set preview_url to true in the message body and
      make sure the URL begins with http:// or https://.
    type: boolean
  template:
    $ref: ./Template.yaml
  text:
    $ref: ./Text.yaml
  to:
    description: The WhatsApp ID (phone number) returned from contacts endpoint.
    type: string
  type:
    $ref: ./MessageType.yaml
  video:
    $ref: ./Video.yaml
  whatsapp_user_id:
    description: The sender's WhatsApp ID.
    type: string
required:
  - to
  - whatsapp_user_id
title: Send Message Request Body
type: object
