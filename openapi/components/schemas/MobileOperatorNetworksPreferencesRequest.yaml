description: >-
  A list of mobile operator networks and the priority that should be applied
  when the SIM is connecting to the network.
items:
  $ref: ./MobileOperatorNetworkPreferencesRequest.yaml
type: array
