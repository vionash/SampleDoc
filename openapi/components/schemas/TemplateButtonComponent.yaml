properties:
  index:
    description: >-
      Position index of the button. You can have up to 3 buttons using index
      values of 0-2.
    title: Index
    type: integer
  parameters:
    allOf:
      - $ref: ./TemplateButtonParameters.yaml
    description: >-
      The parameters for the button, which are set at creation time in your
      Business Manager.
    title: Parameters
  sub_type:
    allOf:
      - $ref: ./ButtonSubtype.yaml
    description: Used when type is set to button.
  type:
    allOf:
      - $ref: ./ButtonComponentType.yaml
    description: Describes the component type.
required:
  - type
  - sub_type
  - index
  - parameters
title: TemplateButtonComponent
type: object
