properties:
  connection_id:
    $ref: ./ConnectionId.yaml
  contents:
    description: >-
      The file you want to upload. The maximum allowed size is 20 MB. contents
      and media_name/media_url can't be submitted together.
    format: binary
    type: string
  from:
    $ref: ./From.yaml
  monochrome:
    $ref: ./Monochrome.yaml
  quality:
    $ref: ./Quality.yaml
  store_media:
    $ref: ./StoreMedia.yaml
  t38_enabled:
    $ref: ./T38Enabled.yaml
  to:
    $ref: ./To.yaml
required:
  - connection_id
  - contents
  - from
  - to
title: Send fax multipart request
type: object
