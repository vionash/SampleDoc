properties:
  components:
    $ref: ./TemplateComponents.yaml
  language:
    $ref: ./Language.yaml
  name:
    description: Name of the template.
    type: string
  namespace:
    description: >-
      Namespace of the template. This must be the namespace associated with the
      WhatsApp business account that owns the phone number associated with the
      current WhatsApp Business API client or the message fails to send.
    type: string
required:
  - namespace
  - name
  - language
title: Template
type: object
