content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/FaxApplication.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: Get All Fax Applications Response
      type: object
description: Successful response
