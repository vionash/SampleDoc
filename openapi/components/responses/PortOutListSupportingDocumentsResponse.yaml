content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/PortOutSupportingDocument.yaml
          type: array
      type: object
description: Portout Supporting Documents
