content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/GcbPhoneNumber.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      type: object
description: A list of phone numbers
