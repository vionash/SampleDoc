content:
  application/json:
    schema:
      properties:
        errors:
          items:
            $ref: ../schemas/UnexpectedError.yaml
          type: array
description: An unexpected error occurred.
