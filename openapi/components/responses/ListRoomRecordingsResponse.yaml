description: List room recordings response.
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: ../schemas/RoomRecording.yaml
        meta:
          $ref: ../schemas/PaginationMeta.yaml
