content:
  application/json:
    schema:
      properties:
        data:
          $ref: ../schemas/CredentialConnection.yaml
      title: Credential Connection Response
      type: object
description: Successful response with details about a credential connection.
