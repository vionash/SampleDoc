content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/FqdnConnection.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List FQDN Connections Response
      type: object
description: Successful response with a list of FQDN connections.
