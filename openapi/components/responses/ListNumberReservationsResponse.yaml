content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/NumberReservation.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List Number Reservations Response
      type: object
description: Successful response with a list of number reservations.
