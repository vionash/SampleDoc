content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/QueueCall.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List Queue Calls Response
      type: object
description: Successful response with a list of calls in a queue.
