content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/CsvDownload.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List CSV Downloads Response
      type: object
description: Successful response with a list of CSV downloads.
