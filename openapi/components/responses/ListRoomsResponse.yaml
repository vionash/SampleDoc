content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/Room.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      type: object
description: List rooms response.
