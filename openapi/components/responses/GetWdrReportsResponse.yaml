content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/WdrReport.yaml
          type: array
      type: object
description: Successful response
