content:
  application/json:
    schema:
      properties:
        data:
          $ref: ../schemas/Ip.yaml
      title: Ip Response
      type: object
description: Successful response with details about an IP.
