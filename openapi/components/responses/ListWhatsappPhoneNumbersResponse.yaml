content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/WhatsappPhoneNumber.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List WhatsApp Phone Numbers Response
      type: object
description: Successful response with a list of WhatsApp Phone Numbers.
