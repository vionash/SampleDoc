content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/PortingOrderDocument.yaml
          type: array
      type: object
description: Successful response
