content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/SimpleSIMCard.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      type: object
description: Successful response
