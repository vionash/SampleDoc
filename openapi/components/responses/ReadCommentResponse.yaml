content:
  application/json:
    schema:
      properties:
        data:
          allOf:
            - $ref: ../schemas/ReadComment.yaml
            - type: object
      type: object
description: A Comment Response
