content:
  application/json:
    schema:
      properties:
        data:
          $ref: ../schemas/BusinessIdentity.yaml
      title: Show Business Identity Response
      type: object
description: Successful response with a Business Identity.
