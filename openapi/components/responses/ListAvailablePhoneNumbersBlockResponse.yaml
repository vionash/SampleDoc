content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/AvailablePhoneNumberBlock.yaml
          type: array
        meta:
          $ref: ../schemas/AvailablePhoneNumbersMetadata.yaml
      title: List Available Phone Numbers Blocks Response
      type: object
description: Successful response with a list of available phone numbers blocks.
