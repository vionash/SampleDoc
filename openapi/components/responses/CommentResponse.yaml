content:
  application/json:
    schema:
      properties:
        data:
          allOf:
            - $ref: ../schemas/Comment.yaml
            - type: object
      type: object
description: A Comment Response
