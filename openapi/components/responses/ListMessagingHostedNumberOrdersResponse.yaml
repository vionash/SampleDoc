content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/MessagingHostedNumberOrder.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List Messaging Hosted Number Order Response
      type: object
description: Successful response with a list of messaging hosted number orders.
