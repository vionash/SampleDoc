content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/BusinessIdentity.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List Business Identities Response
      type: object
description: Successful response with a list of Business Identities.
