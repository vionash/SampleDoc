content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/MessagingUrlDomain.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List Messaging Profile Url Domains Response
      type: object
description: Successful response with details about a messaging URL domain.
