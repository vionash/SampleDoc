content:
  application/json:
    schema:
      properties:
        data:
          $ref: ../schemas/SIMCardNetworkPreference.yaml
      type: object
description: A successful network preferences definition response.
