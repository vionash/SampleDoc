content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/CallEvent.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List Call Events Response
      type: object
description: Successful response with a list of call events.
