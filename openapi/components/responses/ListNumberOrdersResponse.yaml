content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/NumberOrder.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List Number Orders Response
      type: object
description: Successful response with a list of number orders.
