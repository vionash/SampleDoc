content:
  application/json:
    schema:
      properties:
        data:
          $ref: ../schemas/OutboundMessagePayload.yaml
      title: Message Response
      type: object
description: Successful response with details about a message.
