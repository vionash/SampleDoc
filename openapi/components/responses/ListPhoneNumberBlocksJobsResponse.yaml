content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/PhoneNumberBlocksJob.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List Phone Number Blocks Background Jobs Response
      type: object
description: Successful response with a list of phone number blocks background jobs.
