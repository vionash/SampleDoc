content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/Comment.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      type: object
description: An array of Comment Responses
