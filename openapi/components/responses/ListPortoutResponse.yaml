content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/PortoutDetails.yaml
          type: array
        meta:
          $ref: ../schemas/Metadata.yaml
      type: object
description: Portout Response
