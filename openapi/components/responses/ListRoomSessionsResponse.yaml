description: List room sessions response.
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: ../schemas/RoomSession.yaml
        meta:
          $ref: ../schemas/PaginationMeta.yaml
