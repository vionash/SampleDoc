content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/ShortCode.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List Messaging Profile Short Codes Response
      type: object
description: Successful response with a list of messaging profile short codes.
