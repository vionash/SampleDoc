content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/VerifiedCallsDisplayProfile.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List Verified Calls Display Profiles Response
      type: object
description: Successful response with a list of Verified Calls Display Profiles.
