content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/RegulatoryRequirements.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      type: object
description: An array of Regulatory Requiremenst Responses
