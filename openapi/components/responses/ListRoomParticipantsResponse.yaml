description: List room participants response.
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: ../schemas/RoomParticipant.yaml
        meta:
          $ref: ../schemas/PaginationMeta.yaml
