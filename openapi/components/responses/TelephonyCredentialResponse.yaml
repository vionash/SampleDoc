content:
  application/json:
    schema:
      properties:
        data:
          $ref: ../schemas/TelephonyCredential.yaml
      title: Telephony Credential Response
      type: object
description: Successful response with details about a credential
