content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/Ip.yaml
          type: array
      title: List Ips Response
      type: object
description: Successful response with a list of IPs.
