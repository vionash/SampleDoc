content:
  application/json:
    schema:
      properties:
        data:
          $ref: ../schemas/DocReqsRequirementTypeList.yaml
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      type: object
description: Successful response
