content:
  application/json:
    schema:
      properties:
        data:
          $ref: ../schemas/BillingGroup.yaml
      type: object
description: Expected billing group response to a valid request
