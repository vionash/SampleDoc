content:
  application/json:
    schema:
      properties:
        data:
          $ref: ../schemas/NumberOrderWithPhoneNumbers.yaml
      title: Number Order Response
      type: object
description: Successful response with details about a number order.
