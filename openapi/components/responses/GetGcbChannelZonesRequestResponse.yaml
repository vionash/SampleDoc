content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/GcbChannelZone.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      type: object
description: A list of channel zones
