content:
  application/json:
    schema:
      properties:
        data:
          $ref: ../schemas/NumberOrderDocument.yaml
      title: Number Order Document Response
      type: object
description: Successful response with details about a number order document.
