content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/WhatsappBusinessAccount.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List WhatsApp Business Accounts Response
      type: object
description: Successful response with a list of WhatsApp Business Accounts.
