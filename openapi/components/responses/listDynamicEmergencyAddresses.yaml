content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/DynamicEmergencyAddress.yaml
          type: array
        meta:
          $ref: ../schemas/Metadata.yaml
      type: object
description: Dynamic Emergency Address Responses
