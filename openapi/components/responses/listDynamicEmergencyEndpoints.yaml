content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/DynamicEmergencyEndpoint.yaml
          type: array
        meta:
          $ref: ../schemas/Metadata.yaml
      type: object
description: Dynamic Emergency Endpoints Responses
