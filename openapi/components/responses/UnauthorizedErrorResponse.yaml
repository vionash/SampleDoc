content:
  application/json:
    schema:
      properties:
        errors:
          items:
            $ref: ../schemas/UnauthorizedError.yaml
          type: array
description: >-
  The required authentication headers were either invalid or not included in the
  request.
