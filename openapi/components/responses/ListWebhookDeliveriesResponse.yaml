content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/webhook_delivery.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMetaSimple.yaml
      type: object
description: A paginated array of webhook_delivery attempts
