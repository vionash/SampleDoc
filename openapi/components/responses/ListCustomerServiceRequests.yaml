content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/CustomerServiceRequest.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      type: object
description: Successful Response
