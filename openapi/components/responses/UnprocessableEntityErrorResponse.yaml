content:
  application/json:
    schema:
      properties:
        errors:
          items:
            $ref: ../schemas/UnprocessableEntityError.yaml
          type: array
description: Unprocessable entity. Check the 'detail' field in response for details.
