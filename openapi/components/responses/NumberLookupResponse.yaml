content:
  application/json:
    schema:
      properties:
        data:
          $ref: ../schemas/NumberLookupRecord.yaml
      title: Number Lookup Response
      type: object
description: Successful response
