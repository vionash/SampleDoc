content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/PhoneNumbersJob.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List Phone Numbers Background Jobs Response
      type: object
description: Successful response with a list of phone numbers background jobs.
