content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/RoomComposition.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      type: object
description: List room compositions response.
