content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/Conference.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      title: List Conferences Response
      type: object
description: Successful response with a list of conferences.
