content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/ManagedAccount.yaml
          type: array
        meta:
          $ref: ../schemas/PaginationMeta.yaml
      type: object
description: Successful response with a list of managed accounts.
