content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/InventoryCoverage.yaml
          type: array
        meta:
          $ref: ../schemas/InventoryCoverageMetadata.yaml
      type: object
description: Successful response with a list of inventory coverage levels
