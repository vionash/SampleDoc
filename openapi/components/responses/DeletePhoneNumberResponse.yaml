content:
  application/json:
    schema:
      properties:
        data:
          $ref: ../schemas/PhoneNumberDeletedDetailed.yaml
      title: Phone Number Response
      type: object
description: Successful response with details about a phone number.
