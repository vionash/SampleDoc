content:
  application/json:
    schema:
      properties:
        errors:
          items:
            $ref: ../schemas/ForbiddenError.yaml
          type: array
description: >-
  You do not have permission to perform the requested action on the specified
  resource or resources.
