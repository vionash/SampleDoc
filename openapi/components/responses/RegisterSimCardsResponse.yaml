content:
  application/json:
    schema:
      properties:
        data:
          description: Successfully registered SIM cards.
          items:
            $ref: ../schemas/SimpleSIMCard.yaml
          type: array
        errors:
          items:
            $ref: ../schemas/Error.yaml
          type: array
      type: object
description: Successful response
