get:
  operationId: retrieveWhatsappPhoneNumber
  parameters:
    - description: Identifies the WhatsApp Phone Number
      in: path
      name: id
      required: true
      schema:
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            properties:
              data:
                $ref: ../components/schemas/WhatsappPhoneNumber.yaml
            title: WhatsApp Phone Number
            type: object
      description: WhatsApp Phone Number details
    default:
      $ref: ../components/responses/GenericErrorResponse.yaml
  summary: Retrieves a WhatsApp Phone Number
  tags:
    - WhatsApp Phone Number
  x-group-parameters: 'true'
  x-latency-category: responsive
patch:
  operationId: patchWhatsappPhoneNumber
  parameters:
    - description: Identifies the WhatsApp Phone Number
      in: path
      name: id
      required: true
      schema:
        type: string
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/UpdateWhatsappPhoneNumberRequest.yaml
    required: true
  responses:
    '200':
      content:
        application/json:
          schema:
            properties:
              data:
                $ref: ../components/schemas/WhatsappPhoneNumber.yaml
            title: WhatsApp Phone Number
            type: object
      description: Updated WhatsApp Phone Number details
    default:
      $ref: ../components/responses/GenericErrorResponse.yaml
  summary: Update a WhatsApp Phone Number webhook
  tags:
    - WhatsApp Phone Number
  x-group-parameters: 'true'
  x-latency-category: responsive
