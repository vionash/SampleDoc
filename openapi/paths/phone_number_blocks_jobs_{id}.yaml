get:
  operationId: retrievePhoneNumberBlocksJob
  parameters:
    - description: Identifies the Phone Number Blocks Job.
      in: path
      name: id
      required: true
      schema:
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            properties:
              data:
                $ref: ../components/schemas/PhoneNumberBlocksJob.yaml
            title: Phone Number Blocks Job
            type: object
      description: Phone number blocks job details.
    default:
      $ref: ../components/responses/GenericErrorResponse.yaml
  summary: Retrieves a phone number blocks job
  tags:
    - Number Blocks Background Jobs
  x-group-parameters: 'true'
  x-latency-category: responsive
